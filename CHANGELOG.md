# CHANGELOG

## v1.4.1 (2026-07-17)

> 🍦 开发期间吃掉一个三色杯冰淇淋。

### 修复
- **打断位置精确追踪**：修复多段打断时偏移量错误（原方案用 segment 拼接长度对 `fullResponse` 做 `substring`，未计入 `❖❖❖` 标记的字符数），改用预计算位置 + clean→raw 字符映射表
- **mid-segment 打断**：用户在句子显示中途打断时，按已显示的 clean 字符精确查表定位，无论 `[表情]` `{动作}` 标签在开头还是中间都能准确截断
- **记忆身份混淆**：打断注释（含 AI 未显示文本）被错误存入 `role: "user"` 的记忆，导致 AI 文本被当成用户发言。修复为 API 消息与记忆消息分离
- **API 并发竞态**：快速连续发送消息可能触发多个并行 API 调用，回复互相覆盖。添加 `chatLock` 并发守卫
- **导入导出安全**：导入时增加模型匹配验证（导出模型 ≠ 当前选中时弹确认框）、覆盖确认对话框、旧版纯数组 memory 格式兼容
- **托盘导入验证**：补充 JSON 格式校验 + `.moc3` 存在性检查 + 复制后验证 + 失败回滚

### 优化
- **打断后体验**：AI 回复全部播放完毕后自动弹出聊天栏，无需双击
- **播放速度下限**：每字最低 50ms 延迟（~20字/秒），防止长文本显示过快
- **段间间隔动态化**：从固定 400ms 改为 `200-500ms` 按句子长度自适应
- **System Prompt 强化**：明确每段 50-80 字限制，3句以上必须使用 `❖❖❖` 分段

### 意外收获
- **每段独立表情/动作**：`❖❖❖` 分段机制意外实现了"一段一个表情/动作"的效果 — 每个 segment 独立调用 `applyExpression`，标签互不干扰。这本是打断追踪重构时的副产品，测试后发现效果出奇地好，正式收编为特性。

---

## v1.4 (2026-07-16)

### 新增
- **AI 流式回复**：使用 `❖❖❖`（三个黑色菱形）标记将长回复分割，客户端逐字显示，间隔按文本长度动态计算
- **打断功能**：AI "说话"中途输入即可打断，未显示内容以 `[The user interrupted...]` 格式自动注入下一次 API 调用上下文
- **导出/导入角色卡与记忆**：设置界面一键导出为 JSON，导入时自动备份旧文件并支持回滚

---

## v1.3 (2026-06-09)

### 新增
- **鼠标穿透模式**：开启后鼠标点击完全穿透窗口，不遮挡桌面操作
- 穿透时可从托盘开关；右键菜单/双击/聊天栏自动禁用
- 开穿透强制宠物模式，切窗口模式自动关穿透

---

## v1.2 (2026-06-08)

### 新增
- AI 通过 `{动作名}` 标签自主控制 Live2D 动作动画（Motions）
- 动作自动关联音频播放（读取 model3.json Sound 字段）
- 流萤（Firefly）Live2D 模型上传，附带版权声明

### 修复
- 动作 Bezier 曲线正确解析（7 值），修复动作后脸消失/眼睛异常
- 动作结束后恢复播放前参数快照，修复变身/闭眼状态残留
- 动作播放期间表情可正常切换
- 表情/动作缓存切换模型时清理
- model3.json 清理失效 Scene* 引用

### 优化
- 流萤动作文件中文命名（闭眼→眨眼，Scene26710→唱歌等）
- `start.bat` 纯英文防 CMD 乱码

---

## v1.1 (2026-06-08)

### 新增
- AI 通过 `[表情名]` 标签自主控制 Live2D 表情
- 模型加载时无 `character.json` 则自动创建
- 模型无 `icon.png` 时使用内置默认托盘图标
- 设置界面切换模型即时生效

### 修复
- AI 融合角色卡 max_tokens 增至 20000，修复大文本截断
- JSON 解析三级容错：正则 → 补全 → 字段提取
- `start.bat` 首次自动检测并安装依赖

---

## v1.0 (2026-06-07)

### 功能
- 导入任意 Live2D Cubism 3/4 模型，文件夹名自动成为角色名
- 多后端 AI（DeepSeek / Claude / OpenAI / 自定义）
- 加密 API Key 存储
- 分层记忆：短期对话 + AI 自动提取长期事实
- 表情联动：AI 回复关键词触发 Live2D 表情
- 宠物模式（透明置顶）/ 窗口模式（可调大小）
- AI 融合角色卡（支持多文件导入）
- 系统托盘管理
