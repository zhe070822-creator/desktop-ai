# Desktop AI — Live2D 桌面 AI 伙伴

一个基于 Electron + PixiJS 的 Live2D 桌面 AI 程序。导入任意 Live2D Cubism 3/4 模型，配上 AI 后端，你就拥有了一个有性格、会聊天、会做表情的桌面伙伴。

> 这是我随便写着玩的项目，不过会继续完善到真正好用的程度。

## 快速开始

### 第一步：安装 Node.js

前往 [nodejs.org](https://nodejs.org/) 下载并安装 Node.js（LTS 版本即可）。装好后，你的电脑就有了 `npm` 命令。

> 怎么确认装好了？在终端（命令提示符 / PowerShell / Windows 终端）输入 `npm -v`，如果显示版本号（如 `10.x.x`）就是装好了。

### 第二步：下载并启动本项目

1. 从 [Releases](https://github.com/zhe070822-creator/desktop-ai/releases) 下载最新版本的压缩包，解压到任意文件夹
2. 双击文件夹内的 `start.bat` 即可启动（首次会自动安装依赖，之后直接启动）
3. 或者手动：在文件夹地址栏输入 `cmd` 回车，依次输入：
   ```bash
   npm install    # 首次需运行，下载依赖
   npm start      # 启动程序
   ```

### 第三步：开始使用

1. 首次启动弹出设置窗口 → 填入 API Key（支持 DeepSeek / Claude / OpenAI / 自定义）
2. **导入模型**：托盘右键 → 导入模型 → 选择 Live2D 模型文件夹
3. 双击模型 → 打字聊天

## 导入模型

**文件夹名称就是角色名称**。比如文件夹叫 `Alex`，角色名就是 Alex。

模型文件夹需要包含与文件夹同名的 `.model3.json`（如 `Alex/Alex.model3.json`）。

导入时会自动：
- 生成默认角色卡（`character.json`）
- 扫描 `Expressions/` 目录并注册表情
- 尝试去除水印（不一定成功）

### AI 融合角色卡

设置界面可以导入 `.md` / `.json` 角色描述文件，让 AI 将其与当前角色卡融合，生成更完整的角色设定。支持多文件同时导入。

## 功能

- 多模型支持，导入即用
- 多后端 AI（DeepSeek / Claude / OpenAI / 自定义），API Key 加密存储
- 分层记忆：短期对话 + AI 自动提取长期事实
- 表情联动：AI 回复关键词触发 Live2D 表情，4 秒自动恢复
- 宠物模式（透明置顶）/ 窗口模式（可调大小）
- 系统托盘：切换模型、切换模式、导入模型、API 设置

## 待完成

- [✓] **Motions 动作系统**：读取并触发模型的 `.motion3.json` 动画（如变身、睡觉等）
- [✓] **鼠标穿透**：宠物模式/窗口模式下鼠标点击完全穿透，系统托盘可控开关
- [✓] 导出/导入角色卡与记忆
- [✓] **流式 AI 回复 + 打断**：AI 使用 `❖❖❖` 标记将回复分成多段，客户端逐字显示并动态计算间隔。用户可在显示中途输入打断，打断后系统向 API 追加上下文告知被中断的内容。
- [✓] **AI 追问体系**：AI 返回 JSON 信封设定 `follow_up` 和 `wait` 秒数。客户端唯一计时器，AI 说完后倒计时，超时自动追问。追问上下文包含输入栏状态（有/无内容），不泄露用户打字内容。连续追问上限 5 次，打断/新消息自动取消。
- [ ] **扩展规模 — 通用模型导入**：将 API 设置界面和系统托盘的导入选项改为弹出模型导入菜单，支持选择模型类型：图片（静态/动态命名）、视频（1:1 大小）、Live2D、3D（远期，可 AI 自驱动，无需预设动作和表情）。同时添加软件文件夹下的模型注册表（uid、名称、目录、类型、苏醒时间等）。
- [ ] **打包为 exe**：一键安装包，无需装 Node.js 即可运行（远期）
- [ ] **记忆大更新**：突破当前 JSON 线性记忆的局限。参考 [Letta](https://github.com/letta-ai/letta)（star 很多的 AI 记忆库）以及 [claude-subconscious](https://github.com/letta-ai/claude-subconscious)（AI 潜意识）。方向：记忆六维向量场，加权权重，前五维主用、第六维（第六感）预设占位——等 AI 自我空间完成后让 AI 用自身经历填充。目标是像人一样的非线性记忆。
- [ ] **AI 自我空间**：软件文件夹 `自我空间/` 下每个模型拥有独立文件夹（模型添加时创建，移除时不删除，待设置功能处理）。AI 对自我空间拥有绝对权限：可读取/创建任意格式文件、调用工具、像 Claude Code 一样运行可执行文件（如安装 Python 画画）。安全防护：Hook 式越权拦截，AI 无法访问/读写自我空间外的任何内容。被拦截时以 system/安全拦截角色告知 AI 发生了什么。
- [ ] **OCR 图片识别**：仿微信聊天界面完成后，聊天栏支持发送图片 → 客户端 OCR 识别文字 → 追加到用户消息后。使用独立 API 文件，不改动正常聊天 API。
- [ ] **AI 表情包**：AI 可发送自定义表情/贴图（仿微信聊天界面完成后实现）
- [ ] **聊天界面 — 仿微信风格**：对话气泡 + 滚动历史 + 时间戳。可选：左侧多联系人热切换不同模型（可能引入大量 bug，待定）。用户可将文件复制到 AI 自我空间文件夹，在聊天栏粘贴相对路径，AI 调用工具读取。
- [ ] **文本溢出处理**：气泡内超长无换行文本（如连续数字）会溢出显示区域。CSS `overflow-wrap` 可能打断 AI 的 `\n` 排版，JS 智能断句又太复杂。待找更优雅的方案。
- [ ] 多语言支持
- [ ] **多设备兼容**：Windows / macOS / Linux 桌面端，Android / iOS 移动端（远期目标）

## 更新日志

### v1.4.2（2026-07-18）
- **AI 追问系统**：AI 回复后主动继续对话，模拟真人
  - JSON 信封格式：`{"text":"...", "follow_up":true/false, "wait":秒数}`，4 级容错解析
  - 客户端唯一计时器，AI 说完后倒计时（默认 5s，最小 1s）
  - 打字中自动延长 10s（只延一次），输入栏连续 3 次空则判定用户不在
  - 连续追问上限 5 次，用户发消息自动取消
  - 兼容旧版纯文本格式（无追问）
- **Session 日志**：`logs/` 目录，人类可读纯文本，记录 API 请求/响应、追问计时器事件
- 网络错误自动追加"请检查网络"提示
- 已知问题：追问 API 飞行中用户恰好发消息 → 两条并行（概率极低，待加锁）

### v1.4.1.1（2026-07-17）
- **安全修复**：API 连接测试失败时不再暴露响应体（401/403 仅提示 Key 无效）
- 保存前自动测试 API 连接，不通过则阻止保存
- 新增"测试连接"按钮（10s 超时）
- 无角色卡时自动生成默认卡

### v1.4.1（2026-07-17）
- 修复打断位置追踪：多段打断时偏移量计算错误（未计入 `❖❖❖` 标记长度），改用精确位置追踪
- 修复 mid-segment 打断：用户在句子显示中途打断时，未显示文本估算更准确
- 修复 API 并发问题：快速连续发送消息可能导致多个回复互相覆盖，添加并发守卫
- 修复打断后体验：AI 回复播放完毕后自动显示聊天栏，无需双击
- 修复导入导出：添加模型匹配验证、覆盖确认对话框、旧版 memory 格式兼容
- 优化流式显示速度：短句 ~2s 最短显示时间，段间间隔动态计算 (~0.2s~0.5s)
- 强化 AI 分段指令：每段 50-80 字限制，3句以上必须使用 `❖❖❖` 分段
- 托盘菜单导入模型增加 JSON/.moc3 验证

### v1.4
- AI 流式回复：使用 `❖❖❖` 标记分段，客户端逐字显示，间隔动态计算
- 打断功能：在 AI "说话"中途输入即可打断，打断上下文自动注入下一次 API 调用
- 导出/导入角色卡与记忆（设置界面），带备份与回滚

### v1.3
- 鼠标穿透模式：一键让所有点击穿过窗口，不挡桌面/游戏操作

### v1.2
- AI 通过 `{动作名}` 标签自主控制 Live2D 动作动画（含音频）
- 动作播放前后自动快照，结束后恢复原状
- 流萤（Firefly）Live2D 模型随项目附带

### v1.1
- AI 通过 `[表情名]` 标签自主控制表情，不再依赖关键词匹配
- 自动角色卡生成：模型加载时无 `character.json` 则自动创建
- 默认托盘图标：模型无 `icon.png` 时使用内置图标
- 设置界面切换模型即时生效
- AI 融合角色卡 max_tokens 增至 20000，修复大文本截断
- JSON 解析三级容错：正则 → 补全 → 字段提取
- `start.bat` 首次自动检测并安装依赖

### v1.0
- 首个可用版本

## 技术栈

Electron + PixiJS + pixi-live2d-display + Live2D Cubism Core

## 模型版权

本项目包含的流萤（Firefly）Live2D 模型，版权归原作者所有：

- **模型作者**：[@是依七哒](https://space.bilibili.com/)（B站）
- **来源视频**：[前瞻小人模型](https://www.bilibili.com/video/BV1wSyjBFEfD/)

模型仅用于角色演示。如需使用该模型，请尊重原作者版权。

## 免责声明

本软件是一个开源的桌面 AI 伴侣程序。用户在使用本软件时：

- **API 使用**：本软件依赖第三方 AI 服务（如 DeepSeek、Claude、OpenAI 等）。用户需自行获取 API Key 并遵守相应服务商的条款（TOS）。因违反第三方条款产生的后果由用户自行承担。
- **模型版权**：用户导入的 Live2D 模型，其版权归原作者所有。请确保拥有合法使用权后再导入。软件本身不提供任何 Live2D 模型（Firefly 流萤除外，仅作演示用途）。
- **行为责任**：用户通过本软件与 AI 交互产生的所有内容及后果，由用户自行负责。开发者不对用户的任何不当使用承担直接或间接责任。
- 如果你真的逐字读完了这段免责声明，建议你出门走走。或者继续和桌面 AI 聊天，也行。
- 对了，如果你不同意以上条款……放轻松，这只是一个桌面宠物软件。

## 作者

Jestions & Claude

## License

ISC
